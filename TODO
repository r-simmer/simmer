 - inspect the queue (some kind of "advanced peek")
 - change default behavior of `seize` and `release` (for version >=4.0.0, see issues #25, #27)
 - start/stop a generator from inside a trajectory
 - modify resources from inside a trajectory
 - implement a parallel branch activity
 - del_generator, del_resource?
 - add[del]_container (see simpy.resources.container)
 - add[del]_store (see simpy.resources.store)
 - open an organization on GitHub, split the package into core and graphics (release 4.0.0?)